name: Lint Oracle scripts

on:
  push:
    branches: [main]
    paths:
      - 'II/MODBD/sv*/scripts/**'
      - '.github/workflows/modbd_sql_lint.yml'
  pull_request:
    branches: [main]
    paths:
      - 'II/MODBD/sv*/scripts/**'
      - '.github/workflows/modbd_sql_lint.yml'

jobs:
  lint-sql:
    runs-on: ubuntu-latest
    if: |
      (github.event_name == 'push' && contains(github.event.head_commit.message, 'MODBD')) ||
      (github.event_name == 'pull_request' && contains(github.event.pull_request.title, 'MODBD'))
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install sqlfluff
        run: pip install sqlfluff

      - name: Lint SQL sv1
        run: sqlfluff lint --config II/MODBD/.sqlfluff II/MODBD/sv1/scripts
      
      - name: Lint SQL sv2
        run: sqlfluff lint --config II/MODBD/.sqlfluff II/MODBD/sv2/scripts

      - name: Lint SQL sv3
        run: sqlfluff lint --config II/MODBD/.sqlfluff II/MODBD/sv3/scripts

      - name: Lint SQL sv4
        run: sqlfluff lint --config II/MODBD/.sqlfluff II/MODBD/sv4/scripts
