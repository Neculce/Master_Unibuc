## Pre-requiremenrs

- **Docker** 
- **Python 3.10+**

---

## Clone the repo 

After cloning the repo, copy the `.env.example` file to `.env` and fill in the required values.
After that, just run `docker-compose up -d --build` to  build the custom image and start the app.
